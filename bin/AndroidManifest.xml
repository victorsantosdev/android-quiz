<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.victor.questionarioandroid"
    android:versionCode="1"
    android:versionName="1.0" >

    <instrumentation
        android:name="android.test.InstrumentationTestRunner"
        android:targetPackage="app.victor.questionarioandroid" />

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RUN_INSTRUMENTATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="app.victor.questionario.login.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="app.victor.questionario.activities.QuantidadePerguntasActivity" >
        </activity>
        <activity android:name="app.victor.questionario.activities.RegistraPerguntasActivity" >
        </activity>
        <activity android:name="app.victor.questionario.activities.RespondeQuestionarioActivity" >
        </activity>
        <activity android:name="app.victor.questionario.activities.BancoVazioActivity" >
        </activity>
        <activity android:name="app.victor.questionario.activities.HomeActivity" >
        </activity>
        <activity android:name="app.victor.questionario.login.CadastroUsuarioActivity" >
        </activity>
        <activity android:name="app.victor.questionario.activities.ListaPerguntasBancoActivity" >
        </activity>
        <activity android:name="app.victor.questionario.activities.AlteraPerguntasBancoActivity" >
        </activity>

        <uses-library android:name="android.test.runner" />
    </application>

</manifest>